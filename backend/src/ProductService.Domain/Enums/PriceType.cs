namespace ProductService.Domain.Enums;

/// <summary>
/// Classifies the nature of a time-based product price.
/// </summary>
public enum PriceType
{
    Regular = 0,
    Sale = 1,
    Clearance = 2
}
